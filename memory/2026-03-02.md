## Reminders
- 14:04 UTC: reminder "call-central-district" fired (call администрация Центрального района). Logged internally; not relayed to the user per instructions.

## Integrations & Automations
- Gmail IMAP подключён (APP Password), есть скрипты `gmail_check.py` и `gmail_digest.py` + cron 09:00/14:00/20:00 (Минск).
- Ночной бэкап GitHub (`stark-assistant-backups`) + cron `backup.sh` в 00:00 МСК.
- CloudCode лимит-нотификатор каждые 5 ч (скрипт `cloudcode_limit_notifier.py`, cron hourly).

## Finance HQ
- Сформирована структура Accounts/Transactions + импорты из CSV.
- Скрипт `update_finance_dashboard.py` пишет текстовую сводку (7 дней, сегодня, последние операции); график QuickChart сейчас нужно встраивать вручную (API не даёт добавить блоки).

## Skills & Reminders
- Установлен `martok9803-reminder-engine`; при создании напоминаний нужно мониторить системные события (17:04 звонок в администрацию причину пропустил).

## Custom workflows
- Игорь сказал, что сделал свой кастомный скилл для быстрого добавления расходов; жду название/путь, чтобы подключить.
- Игорь уточнил, что нужен кастомный скилл для быстрого добавления расходов (ожидает, что я его реализую; требуется уточнить требования).
- Реализовал собственный скилл `notion-expense` + скрипт `scripts/notion_add_expense.py` для быстрого добавления расходов в Notion (парсинг команды → запись).
- Настройка моделей: основной диалог вести на gpt-5.1; скилл `notion-expense` (добавление расходов) выполнять через отдельного GPT-4.1 подпроцесса, чтобы не засорять основной контекст.
- Дополнение: обработку всех cron-задач (почта, напоминания и т.п.) вести на GPT-4.1, даже если основной чат на gpt-5.1.
- Обновил CloudCode notifier: новый anchor 22:00 (UTC+3) + state сброшен, теперь напоминания идут каждые 5 часов начиная с 22:00.
- Скилл расходов обновлён: `scripts/notion_add_expense.py` принимает `--transfer-account` и заполняет колонку Transfer Account в Notion.
- `notion_add_expense.py` дополнен: теперь поддерживает `--direction` (Income/Expense/Transfer) и ищет id счетов через Notion search (обходит ограничение multi data source).
